<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Cookeet"
             x:Class="Cookeet.MainPage">

    <MasterDetailPage.Resources>
        <ResourceDictionary>
            <Style x:Name="MasterButtons" TargetType="Button">
                <Setter Property="BackgroundColor" Value="#606878"/>
                <Setter Property="BorderColor" Value="#606878"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="VerticalOptions" Value="StartAndExpand"/>
                <Setter Property="HorizontalOptions" Value="StartAndExpand"/>
            </Style>
        </ResourceDictionary>
    </MasterDetailPage.Resources>
    
    <MasterDetailPage.Master>
        <ContentPage>
            <StackLayout x:Name="CookeetMasterStyle" BackgroundColor="#606878">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="4*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="4*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Image Source=""/>
                    <StackLayout Grid.Row="1" Grid.RowSpan="2" Orientation="Horizontal">
                        <Label Text="Oladimeji Oladoyin"/>
                        <Label Text="j"/>
                    </StackLayout>
                </Grid>
                <Button Text="Home" Style="{StaticResource CookeetMasterStyle}"/>
                <Button Text="Recipes" Style="{StaticResource CookeetMasterStyle}"/>
                <Button Text="Places" Style="{StaticResource CookeetMasterStyle}"/>
                <Button Text="Settings" Style="{StaticResource CookeetMasterStyle}"/>
                <Button Text="About" Style="{StaticResource CookeetMasterStyle}"/>
            </StackLayout>
        </ContentPage>
    </MasterDetailPage.Master>

    <MasterDetailPage.Detail>
        <local:HomePage/>
    </MasterDetailPage.Detail>

</MasterDetailPage>
